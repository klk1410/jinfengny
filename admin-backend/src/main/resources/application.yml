server:
  port: 7266
  servlet:
    context-path: /prod-api

spring:
  application:
    name: eco-oil-admin-backend
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://127.0.0.1:3306/jinfeng?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false}
    username: ${SPRING_DATASOURCE_USERNAME:jinfeng}
    password: ${SPRING_DATASOURCE_PASSWORD:djdaWsXnwcrifZmh}
    driver-class-name: ${SPRING_DATASOURCE_DRIVER_CLASS_NAME:com.mysql.cj.jdbc.Driver}
  flyway:
    enabled: true
    locations: classpath:db/migration

# 与 app-backend 必须使用同一库；生产请配置同一 MySQL。
app:
  cors:
    allowed-origins: "*"
  jwt:
    secret: ${APP_JWT_SECRET:envoil-dev-secret-key-must-be-32bytes!!}
    expire-minutes: ${APP_JWT_EXPIRE_MINUTES:10080}
